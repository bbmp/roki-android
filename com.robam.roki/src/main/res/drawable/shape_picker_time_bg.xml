<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/device_new_connected" />
    <!-- 边框 -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />
    <!-- 圆角 -->
    <corners android:radius="@dimen/dp_12" />
    <!-- 边距 -->
    <padding
        android:top="0dp"
        android:bottom="0dp"
        android:left="0dp"
        android:right="0dp" />
</shape>